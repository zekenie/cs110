<span class="label {{#if open}}{{#if assignedTo}}label-primary{{else}}label-warning{{/if}}{{else}}label-default{{/if}}">
	{{#if open}}
		{{#if assignedTo}}
			{{assignedTo.name}}
		{{else}}
			Open
		{{/if}}
	{{else}}
		Closed
	{{/if}}
</span>